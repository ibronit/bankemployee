services:
  postgres:
    image: postgres:16
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=bank_employee

volumes:
  pgdata: